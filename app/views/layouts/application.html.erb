<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8" />
<title>Worket</title>
<meta name="description" content="" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />

<%= stylesheet_link_tag "normalize","estilos", media: "all", "data-turbolinks-track" => true %>
<link href='http://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,400,300,600' rel='stylesheet' type='text/css' />

<%= csrf_meta_tags %>


</head>
<body>
  <header class="fijoFijo">
    <div class="headFijo">
      <figure class="logo">
        <a href="<%= root_path %>"><img src="/assets/logo2.png"></a>
      </figure>
      <nav>
        <ul>
          <% if current_usuario %>
            <li>
              <%= link_to("Salir", destroy_usuario_session_path, :method => :delete) %>
            </li>

          <% else %>
            <li>
              <%= link_to("Entra", usuario_session_path) %>
            </li>
          <% end %>
        </ul>
      </nav>
    </div>
  </header>

  <%= yield %>



  <%= javascript_include_tag "application","script", "data-turbolinks-track" => true %>


  <footer>

  </footer>
</body>
</html>
